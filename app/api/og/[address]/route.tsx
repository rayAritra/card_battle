import {ImageResponse} from "next/og";import {isAddress} from "viem";import {db} from "@/lib/db/client";import type {Card} from "@/types";export const runtime="nodejs";export async function GET(_req:Request,{params}:{params:Promise<{address:string}>}){const {address}=await params;let card:Card|null=null;if(isAddress(address)){const {data}=await db()?.from("wallet_cards").select("card,hide_net_worth").eq("address",address.toLowerCase()).maybeSingle()||{data:null};if(data){card=data.card as Card;card.hideNetWorth=Boolean(data.hide_net_worth)}}return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center",background:"radial-gradient(circle at 50% 20%,#222238,#08080b 55%)",color:"#f4f4f5",fontFamily:"sans-serif"}}><div style={{display:"flex",flexDirection:"column",border:"2px solid #696979",borderRadius:28,padding:54,width:780,background:"linear-gradient(145deg,#181820,#0d0d11)"}}><span style={{fontSize:20,letterSpacing:5,color:"#8a8a94"}}>ONCHAIN BATTLE CARDS</span><div style={{fontSize:76,fontWeight:900,marginTop:28}}>{card?card.archetype:"FORGE YOUR CARD"}</div><div style={{display:"flex",gap:70,fontSize:42,marginTop:30}}><span>LEVEL {card?.level??"?"}</span><span>{card?(card.hideNetWorth?"???":card.netWorth):"Ethereum + Base"}</span></div><div style={{fontSize:24,color:"#999",marginTop:34}}>{card?.tagline??"Turn any wallet into a collectible battle card."}</div></div></div>,{width:1200,height:630})}
