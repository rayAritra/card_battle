import {db} from "@/lib/db/client";import type {Card} from "@/types";export const dynamic="force-dynamic";export default async function Leaderboard(){const {data}=await db()?.from("wallet_cards").select("card").eq("no_index",false).limit(50)||{data:[]};const rows=(data||[]).map(x=>x.card as Card).sort((a,b)=>b.level-a.level);return <main className="page"><p className="kicker">THE HALL OF CHAINS</p><h1>LEADERBOARD</h1><div className="rounds">{rows.length?rows.map((c,i)=><a href={`/card/${c.address}`} className="round" key={c.address}><b>#{i+1}</b><span>{c.archetype}</span><b>LVL {c.level}</b></a>):<p>No cards ranked yet. Forge the first.</p>}</div></main>}
