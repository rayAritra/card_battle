export default function Settings(){return <main className="page"><p className="kicker">SIGNED OWNER CONTROLS</p><h1>PRIVACY SETTINGS</h1><p className="hero-copy" style={{margin:0}}>Hide net worth or opt out of rankings by signing the message below, then submitting it to the settings API.</p><pre className="ability" style={{padding:20,marginTop:25}}>Update Onchain Battle Cards privacy settings</pre><p>POST <code>/api/settings</code> with address, signature, hideNetWorth, and noIndex. No transaction is required.</p></main>}
