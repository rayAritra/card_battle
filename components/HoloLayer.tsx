"use client";import {useRef} from "react";export function HoloLayer(){const raf=useRef(0);return <div className="holo" onPointerMove={e=>{cancelAnimationFrame(raf.current);const el=e.currentTarget.parentElement;if(!el)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width,y=(e.clientY-r.top)/r.height;raf.current=requestAnimationFrame(()=>{el.style.setProperty("--mx",`${x*100}%`);el.style.setProperty("--my",`${y*100}%`);el.style.setProperty("--rx",`${(0.5-y)*18}deg`);el.style.setProperty("--ry",`${(x-.5)*18}deg`)})}} onPointerLeave={e=>{const el=e.currentTarget.parentElement;el?.style.setProperty("--rx","0deg");el?.style.setProperty("--ry","0deg")}}/>}
