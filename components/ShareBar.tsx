"use client";import {motion} from "framer-motion";import type {Card} from "@/types";export function ShareBar({card}:{card:Card}){const text=`My wallet is a Level ${card.level} ${card.archetype}.`;return <motion.div className="share" initial={{opacity:0}} animate={{opacity:1}} transition={{delay:1.9}}><button onClick={()=>navigator.clipboard.writeText(location.href)}>COPY LINK</button><a href={`https://x.com/intent/post?text=${encodeURIComponent(text)}&url=${encodeURIComponent(typeof location!=="undefined"?location.href:"")}`} target="_blank">SHARE ON X</a></motion.div>}
