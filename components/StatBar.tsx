"use client";import {motion} from "framer-motion";import type {StatKey,StatResult} from "@/types";export function StatBar({name,value,delay=0}:{name:StatKey;value:StatResult;delay?:number}){return <motion.details className="stat" initial={{opacity:0}} animate={{opacity:1}} transition={{delay}}><summary><span>{name}</span><i><b style={{width:`${value.score}%`}}/></i><strong>{value.score}</strong></summary><div className="reasons">{value.reasons.join(" · ")}</div></motion.details>}
