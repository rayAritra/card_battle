"use client";import {motion,useReducedMotion} from "framer-motion";import type {Card} from "@/types";import {BattleCard} from "./BattleCard";export function RevealSequence({card}:{card:Card}){const reduced=useReducedMotion();return <motion.div className="reveal" initial={{opacity:0,rotateY:reduced?0:180}} animate={{opacity:1,rotateY:0}} transition={{duration:reduced?.12:.7,ease:[.22,1,.36,1]}}><BattleCard card={card}/></motion.div>}
