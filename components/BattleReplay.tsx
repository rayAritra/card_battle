"use client";import {motion} from "framer-motion";import type {BattleResult,Card} from "@/types";import {BattleCard} from "./BattleCard";export function BattleReplay({a,b,result}:{a:Card;b:Card;result:BattleResult}){return <div><div className="battle-area"><motion.div initial={{x:-100,opacity:0}} animate={{x:0,opacity:1}} transition={{duration:.5}}><BattleCard card={a} compact/></motion.div><div className="vs">VS</div><motion.div initial={{x:100,opacity:0}} animate={{x:0,opacity:1}} transition={{duration:.5}}><BattleCard card={b} compact/></motion.div></div><div className="rounds">{result.rounds.map((r,i)=><motion.div className="round" key={i} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{delay:.5+i*.35}}><b>{r.rollA}</b><span>{r.category}<br/>ROUND {i+1}</span><b>{r.rollB}</b></motion.div>)}</div><motion.h1 className="result" initial={{scale:1.3,opacity:0}} animate={{scale:1,opacity:1}} transition={{delay:2.2}}>VICTORY · {result.winner.slice(0,6)}…</motion.h1><div className="actions"><a href={`?nonce=${result.nonce+1}`}>REMATCH</a><a href="/">GET YOUR OWN CARD</a></div></div>}
