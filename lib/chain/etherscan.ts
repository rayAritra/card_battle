import type { ChainId } from "@/types"; import type { RawTx } from "./shared"; import { resilient } from "./shared";
async function account(address:string,chainId:ChainId,action:string):Promise<RawTx[]>{const key=process.env.ETHERSCAN_API_KEY;if(!key)return [];return resilient(`etherscan:${action}`,async()=>{const url=new URL("https://api.etherscan.io/v2/api");Object.entries({chainid:String(chainId),module:"account",action,address,startblock:"0",endblock:"99999999",page:"1",offset:"3000",sort:"desc",apikey:key}).forEach(([k,v])=>url.searchParams.set(k,v));const response=await fetch(url,{next:{revalidate:86400}});if(!response.ok)throw new Error(`HTTP ${response.status}`);const json=await response.json() as {status?:string;result?:unknown};return Array.isArray(json.result)?json.result.slice(0,3000) as RawTx[]:[]},[])}
export const getTransactions=(a:string,c:ChainId)=>account(a,c,"txlist"); export const getTokenTransfers=(a:string,c:ChainId)=>account(a,c,"tokentx"); export const getNftTransfers=(a:string,c:ChainId)=>account(a,c,"tokennfttx");
