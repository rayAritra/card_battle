import { resilient } from "./shared";
export async function getPrices(contracts:string[]):Promise<Record<string,number>>{if(!contracts.length)return {};return resilient("defillama:prices",async()=>{const ids=contracts.map(x=>`ethereum:${x}`).join(",");const r=await fetch(`https://coins.llama.fi/prices/current/${ids}`,{next:{revalidate:86400}});if(!r.ok)throw new Error(`HTTP ${r.status}`);const j=await r.json() as {coins?:Record<string,{price?:number}>};return Object.fromEntries(Object.entries(j.coins||{}).map(([k,v])=>[k.split(":")[1].toLowerCase(),v.price||0]))},{} as Record<string,number>)}
