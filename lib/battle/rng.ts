import {keccak256,stringToHex} from "viem";export function seedFromInputs(a:string,b:string,date:string,nonce:number){const [x,y]=[a.toLowerCase(),b.toLowerCase()].sort();return keccak256(stringToHex(`${x}:${y}:${date}:${nonce}`))}export function seeded(seed:string){let a=parseInt(seed.slice(2,10),16)>>>0;return()=>{a|=0;a=a+0x6D2B79F5|0;let t=Math.imul(a^a>>>15,1|a);t=t+Math.imul(t^t>>>7,61|t)^t;return((t^t>>>14)>>>0)/4294967296}}
