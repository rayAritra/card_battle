import type {WalletProfile} from "@/types";import {result} from "./scoring";export const scoreDefi=(p:WalletProfile)=>result("defi",p.protocolsTouched.length*Math.max(1,p.protocolCategories.length)*4+Math.sqrt(p.deepestProtocolTxCount)*4,[`${p.protocolsTouched.length} protocols touched`,`${p.protocolCategories.length} protocol categories`,`${p.deepestProtocolTxCount} deep interactions`]);
