import type {WalletProfile} from "@/types";import {result} from "./scoring";export const scoreRisk=(p:WalletProfile)=>result("risk",p.memecoinVolumeShare*100+p.leverageProtocolTxCount*4+p.newContractInteractionCount*.5+p.openApprovalCount*2,[`${Math.round(p.memecoinVolumeShare*100)}% memecoin activity`,`${p.leverageProtocolTxCount} leverage interactions`,`${p.newContractInteractionCount} contracts explored`]);
