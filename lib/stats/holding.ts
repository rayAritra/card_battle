import type {WalletProfile} from "@/types";import {result} from "./scoring";export const scoreHolding=(p:WalletProfile)=>result("holding",p.medianHoldDurationTop5Days*.7+p.longestContinuousHoldDays*.2+p.pctPortfolioUntouched90d*200+100/Math.max(1,p.sellToBuyRatio+1),[`${p.medianHoldDurationTop5Days} day median hold`,`${Math.round(p.pctPortfolioUntouched90d*100)}% untouched for 90d`,`${p.longestContinuousHoldDays} day longest hold`]);
