import "server-only";import {db} from "@/lib/db/client";import {buildWalletProfile} from "@/lib/chain/normalize";import {computeCard} from "@/lib/stats";import type {Card} from "@/types";export async function getCard(address:string):Promise<Card>{const client=db();if(client){const {data}=await client.from("wallet_cards").select("card,hide_net_worth").eq("address",address).maybeSingle();if(data){const card=data.card as Card;card.hideNetWorth=Boolean(data.hide_net_worth);return card}}const card=computeCard(await buildWalletProfile(address));if(client)await client.from("wallet_cards").upsert({address,card,computed_at:new Date().toISOString()});return card}
